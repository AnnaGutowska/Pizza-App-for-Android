package com.example.pizzaapp.backend;

/**
 The Size class contains the  enum type which is a special data type that
 enables for a variable to be a set of predefined constants. In our case, it
 is the various sizes of pizza.
 */
public enum Size {
    small,
    medium,
    large
}
